{
  "mcpServers": {
    "buildplate": {
      "command": "npx",
      "args": ["-y", "github:buildplate/buildplate"],
      "env": {
        "BUILDPLATE_PREVIEW_URL": "http://buildplate.localhost"
      }
    }
  }
}
