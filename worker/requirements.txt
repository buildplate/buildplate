# Buildplate worker — multi-OS (macOS Metal / NVIDIA CUDA / CPU)
# TripoSR is cloned into ~/buildplate/vendor/TripoSR by `npx -y github:buildplate/buildplate setup` (not pip).
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0
pillow>=10.0
numpy>=1.26,<2.3
trimesh>=4.0
manifold3d>=2.5
matplotlib>=3.8
diffusers>=0.30.0,<0.31
transformers>=4.40.0,<4.45
accelerate>=0.33.0
safetensors>=0.4.0
huggingface_hub>=0.24.0,<1.0
onnxruntime>=1.17.0
rembg>=2.0.50
omegaconf>=2.3.0
einops>=0.7.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.0
xatlas>=0.0.9
opencv-python>=4.8.0
fast-simplification>=0.1.7
pymeshfix>=0.17.0
# Marching cubes for TripoSR — installed by `npx -y github:buildplate/buildplate setup` (not this file).
# Mac Metal: CPU build. NVIDIA CUDA 12.9+: setup uses USE_SYSTEM_NVTX / CPU fallback
# so we never depend on the removed CUDA::nvToolsExt library.
