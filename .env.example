# Environment

Copy to `.env` locally if you load env from a file (MCP config `env` block is enough for Cursor).

```bash
BUILDPLATE_WORKER_URL=http://127.0.0.1:8081
BUILDPLATE_WORKER_SECRET=replace-me
# BUILDPLATE_OUT_DIR=~/buildplate/out
# BUILDPLATE_PREVIEW_URL=http://127.0.0.1:3920
```
